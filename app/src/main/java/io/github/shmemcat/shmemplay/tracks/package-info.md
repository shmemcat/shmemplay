# tracks

Phase 4 owns audio-library permission policy, MediaStore candidate access,
deterministic identity resolution, and approved-alias revalidation.

Automatic resolution requires exactly one candidate at a proving evidence
tier. Presentation order is never a tie-breaker. This package must not persist
temporary GoneMAD URIs or provide playlist mutation.

