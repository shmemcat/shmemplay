# persistence

Phase 4 introduces Room schema version 1 for user-approved track aliases.

Alias rows contain evidence fingerprints and MediaStore identities. Temporary
share URIs, URI tokens, and audio bytes must never be persisted.

